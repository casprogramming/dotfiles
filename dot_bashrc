#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
alias grep='grep --color=auto'
PS1='[\u@\h \W]\$ '
export PATH="$PATH:/home/casper/.cargo/bin"
export PATH="$PATH:/home/casper/pycharm/pycharm-2024.3.5/bin"
export EDITOR="nvim"
